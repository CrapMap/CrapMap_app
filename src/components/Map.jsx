import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Map = () => {
  return (
    <View>
      <Text>Display the map here</Text>
    </View>
  );
};

export default Map;

const styles = StyleSheet.create({});
